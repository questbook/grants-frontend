schema:
  - https://the-graph.questbook.app/subgraphs/name/qb-subgraph-goerli-testnet?version=pending
documents:
  - ./graphql/*.graphql
overwrite: true
generates:
  ./src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      scalars:
        BigDecimal: string
        BigInt: string
        Bytes: string
      skipTypename: false
      withComponent: false
      withHooks: true
      withHOC: false
      withRefetchFn: false