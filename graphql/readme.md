# Auto Generating GraphQL Code

## Adding a New Query

1. Create a new .graphql file in the `graphql` folder
2. Name the query with the same name of the file
	- ensure each file only has one query
3. Run `npm run generate`