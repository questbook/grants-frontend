query getApplicationDetails($applicationID: ID!) {
  grantApplication(
    id: $applicationID,
  subgraphError:allow) {
    id
    fields {
      id
      values {
        value
      }
    }
    milestones {
      id
      title
      amount
    }
    grant {
      id
      title
      funding
      workspace {
        id
        title
        logoIpfsHash
        supportedNetworks
      }
      reward {
        id
        asset
        committed
      }
    }
    applicantId
    state
    feedbackDao,
    feedbackDev,
    createdAtS
    updatedAtS
  }
}