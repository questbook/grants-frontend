import { Flex } from '@chakra-ui/react'

function Milestones() {
	const buildComponent = () => {
		return (
			<Flex
				px={5}
				py={4}>
				Milestones
			</Flex>
		)
	}

	return buildComponent()
}

export default Milestones