# This query fetches all the grants for a given domain
query getGrants($domainID: String!) {
    grants(where: {workspace: $domainID}, orderBy: createdAtS, orderDirection: desc, first: 1) {
        id
        title
        acceptingApplications
    }
}