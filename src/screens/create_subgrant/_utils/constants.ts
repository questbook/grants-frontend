export const requiredRubrics = ['Team competence', 'Idea Quality', 'Relevance to our ecosystem']

// eslint-disable-next-line no-restricted-syntax
export enum PayoutMode {
    IN_ONE_GO = 'in one go',
    BASED_ON_MILESTONE = 'based on milestone'
}