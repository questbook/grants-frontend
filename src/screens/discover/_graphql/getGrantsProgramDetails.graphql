query getGrantsProgramDetails($workspaceID: String!) {
    grantsProgram: grants(where: {workspace: $workspaceID}, orderBy: createdAtS, orderDirection: desc) {
        id
        title
        workspace {
            id
            title
        }
    }
}
